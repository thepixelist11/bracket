import { ASTSExprNode } from "../../parser/ast.js";

export function unexpandCond(ast: ASTSExprNode): string[] | false {
    // return new ASTSExprNode(
    //     ASTIdent("if"),
    //     test,
    //     value,
    //     rest.length > 0 ? new ASTSExprNode(
    //         TokenIdent("cond"),
    //         ...rest,
    //     ) : ASTVoid(),
    // )

    // TODO:

    return false;
}

