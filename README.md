# Bracket

### Notice:
The current `main` branch is currently a work-in-progress total rewrite of the
Bracket compiler. Older, working versions are on the `old` branch for the
tree-walking interpreter and the `compiler-old` branch for the bytecode-based
version of Bracket.
